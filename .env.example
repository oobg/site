# 사이트 URL (sitemap.xml 절대 URL에 사용). 없으면 sitemap 생성 스킵, 빌드는 성공.
# VITE_SITE_URL=https://raven.kr

# 개발 서버에서 /api 요청을 프록시할 백엔드 URL (예: http://localhost:8080). 없으면 프록시 비활성화.
# VITE_API_PROXY_TARGET=http://localhost:8080

# 프로덕션에서 API 호출 시 사용할 베이스 URL. 없으면 상대 경로(/api) 사용.
# VITE_API_BASE=

# MSW 활성화 (DEV && VITE_MSW=true 시에만)
# VITE_MSW=true
